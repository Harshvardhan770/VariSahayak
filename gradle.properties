org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Deliberately absent (see plans/00-api-contract.md §0.10):
#   android.enableJetifier      - defaults to false; removal planned in AGP 10
#   android.builtInKotlin=false - removed in AGP 10; never opt out on a new project
#   android.newDsl=false        - same
