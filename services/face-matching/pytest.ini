[pytest]
testpaths = tests
# The service modules are flat at the package root, so the suite adds the service directory
# to sys.path itself (see tests/conftest.py) rather than relying on rootdir inference.
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
